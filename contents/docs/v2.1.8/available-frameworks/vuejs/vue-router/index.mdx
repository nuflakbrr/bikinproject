---
title: Vue Router
description: Learn about the Vue Router framework template.
---

## Vue Router Template Starter Kit

This is a [Vue.js](https://vuejs.org/) project bootstrapped with [`create-vue`](https://github.com/vuejs/create-vue). And generated by [`create-bikinproject-app`](https://github.com/nuflakbrr/create-bikinproject-app) CLI.

## Technologies Used

- [Vue.js](https://vuejs.org/)
- [Vue Router](https://router.vuejs.org/)
- [TailwindCSS](https://tailwindcss.com/)
- [Axios](https://axios-http.com/)
- [ESLint & Prettier](https://eslint.org/)

## Features

The features included in this project template are:

- **Project Architecture**: Clear separation of components using the `Common` and `Mixins` patterns.
- **Modern Stack**: Using the latest versions of Vue.js and Vite.
- **Vite Build**: Extremely fast development experience.
- **Custom Hooks**: A collection of useful hooks such as `useDebounce`, `usePagination`, and `useSort`.
- **Dark/Light Mode**: Built-in support for theme switching.

### Project Architecture

There are several important points regarding how to implement a proper project architecture. For this case study, I have created a customized blank project template that implements best practices for organizing a project architecture to keep it clean.

```
/
├── public/              # Static assets (images, fonts, etc.)
├── src/
│   ├── app/             # File-based Routing (Pages)
│   │   ├── (auth)/      # Route group for authentication
│   │   ├── (root)/      # Route group for main pages
│   │   └── ...          # Page components (.vue)
│   ├── components/      # UI Components
│   │   ├── Common/      # Atomic/Small components
│   │   └── Mixins/      # Complex components (combined Common)
│   ├── data/            # Static data & Metadata
│   ├── hooks/           # Custom Vue Composables
│   ├── interfaces/      # TypeScript Interfaces
│   ├── lib/             # Utilities & Library helpers
│   ├── layouts/         # Layout components
│   └── providers/       # Context/State providers
├── eslint.config.ts     # ESLint configuration
├── tsconfig.json        # TypeScript configuration
├── vite.config.ts       # Vite configuration
├── package.json         # Dependencies & Scripts
└── README.md
```

#### UI Components

- **Common Folder**: Contains atomic components such as buttons, icons, or other reusable basic UI elements.
- **Mixins Folder**: Contains larger and more complex components that are combinations of several `Common` components, such as Navbar or Footer.

### Custom Hooks

Several hooks are provided to speed up development:

- `useDebounce`: To handle inputs that require delayed actions.
- `usePagination`: To handle page navigation via query params.
- `useSort`: To handle data sorting logic.

## Getting Started

This template is available on BikinProject! How to use this template, follow these simple steps:

<Stepper>
<StepperItem title="Install BikinProject">
Start by installing BikinProject on your machine.

```bash
npx create-bikinproject-app@latest
```

</StepperItem>

<StepperItem title="Tell BikinProject Where to Create the Project">
Input a directory where you want to create your project.

```
┌   create-bikinproject-app
│
◇  Where should we create your project?
└  ./your-project # Input your project name, e.g. my-project or .
```

</StepperItem>

<StepperItem title="Choose a Starter Project">
Choose a starter project that suits your needs.

```
┌   create-bikinproject-app
│
◇  Where should we create your project?
│  ./your-project
│
◇  Pick a project type within "./your-project"
│  ○ Next.js
│  ○ Tanstack Router
│  ○ Tanstack Start
│  ● Vue.js
│  ○ Nuxt.js
│  ○ etc.
└  # Select a project type by using the arrow keys
```

</StepperItem>

<StepperItem title="Wait for BikinProject to Create the Project">
Wait for BikinProject to create the project for you.

```
┌   create-bikinproject-app
│
◇  Where should we create your project?
│  ./your-project
│
◇  Pick a project type within "./your-project"
│  Vue.js
│
◇  ⏳ Creating project...
```

</StepperItem>

<StepperItem title="Choose include install dependencies">
Choose to install dependencies or not.

<Note title="Note" type="note">
  If you choose to install dependencies, BikinProject will install the necessary
  dependencies for you. Please wait for the installation to complete.
</Note>

```
┌   create-bikinproject-app
│
◇  Where should we create your project?
│  ./your-project
│
◇  Pick a project type within "./your-project"
│  Vue.js
│
◇  ⏳ Creating project...
│
◇  ✅ Project created successfully!
│
◇  Do you want to install dependencies for ./your-project?
│  ● Yes / ○ No
└  # Select Yes or No by using the arrow keys
```

</StepperItem>

<StepperItem title="Choose package manager">
Choose package manager to install dependencies.

```
┌   create-bikinproject-app
│
◇  Where should we create your project?
│  ./your-project
│
◇  Pick a project type within "./your-project"
│  Vue.js
│
◇  ⏳ Creating project...
│
◇  ✅ Project created successfully!
│
◇  Do you want to install dependencies for ./your-project?
│  Yes
│
◇  Pick a package manager to install dependencies for "./your-project"
│  ● NPM
│  ○ Yarn
│  ○ Bun
│  ○ PNPM
└  # Select by using the arrow keys
```

</StepperItem>

<StepperItem title="Wait for BikinProject to Install Dependencies">
Wait for BikinProject to install the dependencies for you.

```
┌   create-bikinproject-app
│
◇  Where should we create your project?
│  ./your-project
│
◇  Pick a project type within "./your-project"
│  Vue.js
│
◇  ⏳ Creating project...
│
◇  ✅ Project created successfully!
│
◇  Do you want to install dependencies for ./your-project?
│  Yes
│
◇  Pick a package manager to install dependencies for "./your-project"
│  PNPM
│
◇  📦 Installing dependencies using pnpm...
│
◇  ✅ Dependencies installed successfully!
│
◇  🎉 Project ready to use!
│
◇  Next steps. ───────────────╮
│                             │
│  cd ./your-project          │
│  pnpm run dev               │
│                             │
│  Happy Coding!              │
│                             │
├─────────────────────────────╯
│
└  Have a Problems? Report to https://github.com/nuflakbrr/bikinproject/issues
```

</StepperItem>

<StepperItem title="Start Building Your Project">
  After BikinProject generate the starter project, you can start building your
  project.
</StepperItem>

<StepperItem title="Copy Environment Variable">
Copy the `environment variable` with the following command

```bash
cp .env.example .env
```

</StepperItem>

<StepperItem title="Open Development Server">
Open [http://localhost:3000](http://localhost:3000) in your browser and see the result.

You can start editing the page by modifying `/src/routes/(root)/index.tsx`. The page updates automatically as you edit the file.

</StepperItem>

</Stepper>

## Learn More

To learn more about Vue.js, see the following resources:

- [Documentation Vue.js](https://vuejs.org/guide/) - interactive Vue.js tutorials.

You can check out the [Vue.js GitHub Repository](https://github.com/vuejs/vue/) - your feedback and contributions are welcome!

## Author of This Template

Contributor names and contact info,

Naufal Akbar Nugroho  
[Website](https://nuflakbrr.github.io)
[Github](https://github.com/nuflakbrr)
[Instagram](https://instagram.com/kbrnugroho)
